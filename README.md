# Host branch

Currently, this branch is used to host text files, and store static configurations. Specific configurations can only import files from URLs (vesktop's quick css), and other config files are used to store last state, which should not be saved in the main repo.
